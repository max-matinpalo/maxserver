// Comment task in to autostart server on dir open
{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "devserver 📟",
			"type": "shell",
			"command": "npm run dev",
			"presentation": {
				"reveal": "always",
				"panel": "dedicated"
			},
			"runOptions": {
				"runOn": "folderOpen"
			}
		},
		{
			"label": "openBrowser",
			"type": "shell",
			"command": "sleep 1; open http://localhost:3000/docs",
			"presentation": {
				"reveal": "never",
				"panel": "shared"
			},
			"runOptions": {
				"runOn": "folderOpen"
			}
		}
	]
}